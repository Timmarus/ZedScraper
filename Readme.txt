1. Install Python 3.5
2. Install the following dependencies using pip:
requests
bs4
selenium
openpyxl
3. Download PhantomJS and copy the folder into the same directory as the script, rename the folder "phantomjs"
4. Edit your settings by right-clicking the script files and clicking "Edit with IDLE"
5. Scroll to the bottom, all the settings should be there.